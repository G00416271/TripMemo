function Create() {
  return (
    <h1
      style={{
        position: "fixed",
        top: 0,
        width: "100%",
        zIndex: 1000,
        backgroundColor: "lightblue", // optional — just to show it clearly
        margin: 0,
        padding: "10px",
      }}
    >
      Hello from Create
    </h1>
  );
}

export default Create;
