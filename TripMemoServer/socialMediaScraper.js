function crossReferencedata(){ //accept location data(eg,galway, ireland. )

    //const location = (galway, ireland) - in args

    //run through graph api from instagram 

    //pool together tokens, maybe images 
}